[project]
name = "crm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "hdm",
    "hypercorn>=0.17.3",
    "starlette>=0.41.2",
]

[tool.uv.sources]
hdm = { path = "../../", editable = true }
